<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.suellencolangelo.tecnonutriconsumer">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".base.DefaultApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".item.detail.ItemDetailsActivity" />

        <activity android:name=".author.profileDetail.ProfileDetailActivity" />

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="11b82aed39d5740ce0a15b0cd7fbe24b49e940b9" />
    </application>

</manifest>